apiVersion: v1
kind: Service
metadata:
  name: {{ include "cirydwib.fullname" . }}
  labels:
    {{- include "cirydwib.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cirydwib.selectorLabels" . | nindent 4 }}
